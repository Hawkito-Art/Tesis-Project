# Iteración 4 — Reports

## Objetivo
Completar los endpoints que hoy devuelven `501` y restaurar la salida funcional de reportes.

## Tareas
- Identificar endpoints que devuelven `501` y completar sus handlers.
- Verificar payloads con `build_report_payload()` y `build_stats_payload()`.
- Añadir pruebas API para los endpoints restaurados.

## Estimación
1 a 2 horas.

## Criterios de aceptación
- Los endpoints ya no retornan `501`.
- Los payloads devueltos son válidos.
- Los tests de integración de reportes pasan.
